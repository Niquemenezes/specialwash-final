# === Seguridad ===
JWT_SECRET_KEY=change-this
ADMIN_SETUP_TOKEN=change-this-too


# === Origen del frontend ===
FRONTEND_ORIGIN=http://localhost:3000


# === Base de datos ===
# Para Render/Producción usarás Postgres de Render (conexion tipo postgresql://...)
# Si se omite, se usa SQLite local en /tmp/specialwash.db
DATABASE_URL=